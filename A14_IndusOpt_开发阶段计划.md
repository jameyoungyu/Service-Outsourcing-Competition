# A14 流程工业建模数据智能优选与闭环寻优系统开发计划

## 1. 项目基本信息

### 1.1 项目名称

**IndusOpt 工模智优**

副标题：

> 面向流程工业系统辨识的自主建模数据工程师 Agent

### 1.2 项目定位

面向流程工业建模工程师，通过自然语言驱动可信工业算法，自动完成：

- 工业时序数据质量诊断；
- 数据清洗与时间规整；
- 高动态、高信噪比区间筛选；
- 输入输出时滞分析与补偿；
- 多变量共线性分析与冗余变量处理；
- ARX 系统辨识；
- 预处理参数闭环自动寻优；
- 建模数据集、模型参数和分析报告导出。

### 1.3 核心业务闭环

```text
上传或生成工业时序数据
→ 数据质量诊断
→ 数据清洗与时间规整
→ 动态有效区间优选
→ 时滞分析与补偿
→ 共线性分析与变量筛选
→ ARX 系统辨识
→ 模型评价
→ 闭环参数寻优
→ Agent 自动编排
→ 输出优选数据集、模型和报告
```

---

# 2. 开发人员与 AI 分工

本项目由一人开发，使用 GPT 和 Gemini 作为两个独立的编码助手。

## 2.1 GPT 负责

GPT 主要负责后端、算法和系统架构，包括：

- 项目总体架构；
- Python 后端；
- PostgreSQL 数据库；
- 数据处理算法；
- 工业时序数据清洗；
- 动态区间识别；
- 时滞分析；
- 共线性分析；
- ARX 系统辨识；
- Optuna 闭环寻优；
- Agent 工作流编排；
- OpenAPI 接口；
- 单元测试；
- 集成测试；
- Docker 部署；
- 后端技术文档。

## 2.2 Gemini 负责

Gemini 主要负责前端和可视化，包括：

- 产品页面结构；
- UI/UX 设计；
- Vue 3 前端；
- TypeScript 类型定义；
- ECharts 数据可视化；
- API 对接；
- 加载状态；
- 空状态；
- 错误状态；
- 响应式适配；
- 前端测试；
- 演示页面优化。

## 2.3 开发者本人负责

开发者本人必须负责：

- 冻结需求和算法口径；
- 审核 GPT 生成的算法；
- 检查数学公式和指标；
- 确认 API 后再让 Gemini 开发；
- 本地运行全部代码；
- 前后端联调；
- 核查前端是否使用真实 API；
- 处理 GPT 和 Gemini 之间的接口冲突；
- 保存实验和测试结果；
- Git 版本管理；
- 演示视频录制；
- PPT 和答辩材料制作；
- 最终版本裁剪和发布。

## 2.4 协作原则

必须遵守以下原则：

1. GPT 和 Gemini 不同时修改同一个文件。
2. GPT 不修改 `frontend` 目录。
3. Gemini 不修改 `backend` 目录。
4. 后端 API 冻结后，Gemini 才开始对应页面开发。
5. Gemini 不得自行虚构 API 或字段。
6. 前端不得使用固定假数据冒充真实算法结果。
7. 所有核心指标必须由后端实际代码计算。
8. 每个阶段通过验收后再进入下一阶段。
9. 每个阶段结束后执行 Git 提交并打标签。
10. 禁止一次性让 AI 重写整个项目。

---

# 3. 推荐项目目录

```text
indusopt/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   ├── core/
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   ├── repositories/
│   │   ├── workers/
│   │   └── main.py
│   ├── algorithms/
│   │   ├── cleaning/
│   │   ├── segmentation/
│   │   ├── delay/
│   │   ├── collinearity/
│   │   ├── identification/
│   │   ├── optimization/
│   │   └── evaluation/
│   ├── simulation/
│   ├── tests/
│   ├── alembic/
│   ├── scripts/
│   ├── pyproject.toml
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── types/
│   │   ├── utils/
│   │   └── views/
│   ├── tests/
│   ├── package.json
│   └── Dockerfile
├── data/
│   ├── simulation/
│   ├── samples/
│   ├── uploads/
│   └── results/
├── docs/
│   ├── requirements/
│   ├── architecture/
│   ├── algorithms/
│   ├── api/
│   ├── testing/
│   └── competition/
├── reports/
├── docker-compose.yml
├── README.md
├── PHASE_STATUS.md
├── CHANGELOG.md
├── LICENSES.md
└── .env.example
```

---

# 4. 总体阶段划分

| 阶段 | 名称 | 预计时间 | 核心交付物 |
|---|---|---:|---|
| 阶段 0 | 需求、口径与边界冻结 | 2～3 天 | 需求、算法和评测协议 |
| 阶段 1 | 工程骨架与接口契约 | 3～4 天 | 前后端骨架、OpenAPI |
| 阶段 2 | 仿真数据与 ARX 基线 | 5～7 天 | 仿真数据、ARX 基线 |
| 阶段 3 | 数据集管理与质量诊断 | 4～5 天 | 上传、字段配置、质量报告 |
| 阶段 4 | 数据清洗与时间规整 | 6～8 天 | 清洗算法和数据版本 |
| 阶段 5 | 动态有效区间优选 | 6～8 天 | 区间识别和质量评分 |
| 阶段 6 | 时滞与共线性处理 | 6～8 天 | 时滞补偿、变量筛选 |
| 阶段 7 | 完整系统辨识 | 6～8 天 | ARX 模型和残差检验 |
| 阶段 8 | 闭环参数寻优 | 7～10 天 | Optuna 自动寻优 |
| 阶段 9 | Agent 智能编排 | 6～8 天 | 自然语言工作流 |
| 阶段 10 | 报告与数据交付 | 4～6 天 | PDF、CSV、模型和日志 |
| 阶段 11 | 测试与竞赛交付 | 7～10 天 | 实验、视频、PPT、发布包 |

---

# 4.1 差异化创新增补（INNO-1.0）

本计划原始版本覆盖赛题的**字面要求**。由于赛题任务清单本身就是一份实现说明书，仅按字面实现会与其他队伍高度同质化。差异化创新点定义在 `docs/innovation/differentiation-blueprint.md`（`INNO-1.0`），算法口径变更定义在 `docs/algorithms/algorithm-specification-v2.md`（`ALG-0.2`），实验证据在 `docs/experiments/identifiability-ablation.md`（`EXP-1.1`）与 `docs/experiments/self-benchmark.md`（`EXP-2.1`）。

七个创新点与阶段的对应关系：

| # | 创新点 | 落地阶段 | 排期增量 | 状态 |
|---|---|---|---:|---|
| 1 | 可辨识性驱动的数据优选 IDS（D-最优子模） | 阶段 2、5 | +1.5 天 | **原型已完成并通过测试** |
| 2 | 控制相关的模型验收（自由仿真 FIT 为主指标） | 阶段 7、8 | +1.5 天 | **原型已完成并通过测试** |
| 3 | 血缘即缓存 + 多保真寻优 | 阶段 8 | +1.5 天 | 待实现 |
| 4 | 自演进策略记忆库 | 阶段 8、9 | +1.5 天 | 待实现 |
| 5 | 零幻觉报告 + 合规证明 | 阶段 9、10 | +3 天 | 待实现 |
| 6 | 先验知识注入的约束辨识 | 阶段 6、7 | +1.5 天 | 待实现 |
| 7 | 一键自评测基准 | 阶段 11 | +2 天 | 待实现 |

合计 **+13.5 天**，需通过压缩阶段 10 报告样式打磨与阶段 11 性能测试深度吸收。

**若时间不足，按此顺序降级**（依据 `INNO-1.0` §10.2）：必保创新 1、2、7；其次创新 5、4；创新 3 可退化为仅 Hyperband 剪枝，创新 6 可退化为仅符号约束 + Trial 拒绝。

**已落盘的原型**（不影响阶段 1–3 既有交付物）：

```text
backend/algorithms/identifiability/{regressor,excitation,selection,validation}.py
backend/tests/test_identifiability.py          27 passed
backend/scripts/benchmark_identifiability.py   消融实验执行器
docs/experiments/identifiability-ablation.json 10 组种子的原始结果
```

---

# 5. 阶段 0：需求、口径和边界冻结

## 5.1 阶段目标

在正式编码前，冻结：

- 项目解决的问题；
- 系统边界；
- 数据输入格式；
- 算法范围；
- 模型范围；
- 评价指标；
- Agent 权限；
- 前后端职责；
- 每个阶段的验收条件。

本阶段不开发正式页面，不实现 Agent。

## 5.2 第一版固定口径

| 项目 | 第一版决定 |
|---|---|
| 工业对象 | 离散时间动态系统 |
| 输入输出 | 多输入单输出 MISO |
| 数据格式 | CSV |
| 时间字段 | 单时间戳列 |
| 输入变量 | 一个或多个过程输入变量 |
| 输出变量 | 一个目标输出变量 |
| 基础模型 | ARX |
| 参数估计 | 最小二乘、岭回归 |
| 数据划分 | 按时间顺序划分 |
| 闭环优化 | Optuna TPE |
| 后端框架 | FastAPI |
| 前端框架 | Vue 3 + TypeScript |
| 数据库 | PostgreSQL |
| Agent | 白名单工具调用 |
| 大模型 | DeepSeek 或通义千问 |
| 部署方式 | Docker Compose |
| 数据来源 | 自研仿真数据为主，公开数据为补充 |

## 5.3 第一版暂不实现

- 完整 MIMO 系统；
- 非线性系统辨识；
- LSTM、Transformer 等深度辨识模型；
- 实时工业流数据；
- PLC 接入；
- OPC UA 接入；
- 数字孪生；
- 故障诊断；
- 预测预警；
- 任意 Python 代码执行；
- 多租户；
- 复杂组织架构；
- 移动端 App。

## 5.4 GPT 任务

GPT 需要生成：

```text
docs/requirements/official-requirements.md
docs/requirements/scope-freeze.md
docs/requirements/use-cases.md
docs/architecture/system-architecture.md
docs/architecture/data-flow.md
docs/algorithms/algorithm-specification.md
docs/testing/evaluation-protocol.md
docs/api/openapi-plan.md
```

文档必须明确：

1. 官方要求与自加功能。
2. 每个算法的输入、输出、参数和异常。
3. 数据版本追踪方式。
4. 训练、验证、测试划分方式。
5. 系统评价指标。
6. Agent 可调用工具。
7. Agent 禁止执行的操作。
8. 各阶段验收标准。
9. 失败回滚策略。
10. 主要风险。

## 5.5 Gemini 任务

Gemini 本阶段只进行设计，不写完整业务代码。

需要完成：

- 用户角色分析；
- 页面信息架构；
- 用户操作流程；
- 低保真页面草图；
- 工业软件设计规范；
- 页面状态定义；
- 图表使用规划。

建议页面：

```text
登录页
工作台
数据集管理
数据上传与字段配置
数据质量诊断
数据清洗
动态区间优选
时滞分析
共线性分析
系统辨识
闭环寻优
Agent 工作台
运行记录
报告中心
系统设置
```

## 5.6 阶段验收

- [ ] 需求文档完整。
- [ ] 官方要求和自加功能已区分。
- [ ] 系统范围无明显歧义。
- [ ] 页面与后端模块一一对应。
- [ ] 输入输出结构已定义。
- [ ] 算法指标已定义。
- [ ] Agent 权限已冻结。
- [ ] 第一版不做内容已明确。

## 5.7 Git 标签

```text
v0.1.0-requirements
```

---

# 6. 阶段 1：工程骨架与前后端契约

## 6.1 阶段目标

完成可运行的前后端工程骨架，并冻结统一 API 规范。

## 6.2 GPT 任务

搭建后端：

- Python 3.11 或 3.12；
- FastAPI；
- Pydantic 2；
- SQLAlchemy 2；
- Alembic；
- PostgreSQL；
- pytest；
- Ruff；
- Mypy；
- 结构化日志；
- request_id；
- 统一异常响应；
- 健康检查；
- Docker Compose。

初始接口：

```text
GET /api/v1/health/live
GET /api/v1/health/ready
GET /api/v1/system/info
```

统一响应格式：

```json
{
  "success": true,
  "data": {},
  "error": null,
  "request_id": "request-uuid"
}
```

统一错误格式：

```json
{
  "success": false,
  "data": null,
  "error": {
    "code": "DATASET_NOT_FOUND",
    "message": "数据集不存在",
    "details": {}
  },
  "request_id": "request-uuid"
}
```

输出：

```text
backend/openapi.json
docs/api/api-conventions.md
docs/api/error-codes.md
```

## 6.3 Gemini 任务

搭建前端：

- Vue 3；
- Vite；
- TypeScript；
- Vue Router；
- Pinia；
- Axios；
- Element Plus；
- ECharts；
- ESLint；
- Prettier；
- Vitest。

完成：

- 主布局；
- 左侧导航；
- 顶部栏；
- 面包屑；
- 403 页面；
- 404 页面；
- 500 页面；
- 全局加载状态；
- 全局错误提示；
- Axios 拦截器；
- API 类型目录；
- 基础主题规范。

## 6.4 开发者任务

- [ ] 本地启动 Docker Compose。
- [ ] 检查 PostgreSQL 正常。
- [ ] 检查后端健康接口。
- [ ] 检查前端可以访问后端。
- [ ] 检查跨域配置。
- [ ] 检查 `.env` 未提交。
- [ ] 固定端口和启动命令。
- [ ] 检查页面刷新后路由正常。

## 6.5 验收命令

```bash
docker compose up --build

cd backend
pytest
ruff check .
mypy app

cd ../frontend
pnpm test
pnpm build
```

## 6.6 Git 标签

```text
v0.2.0-foundation
```

---

# 7. 阶段 2：仿真数据生成与 ARX 基线

## 7.1 阶段目标

建立具有已知真值的工业时序仿真数据，并实现最基础的 ARX 系统辨识。

## 7.2 GPT 任务

### 场景 S1：基础单输入系统

要求：

- 单输入单输出；
- 已知 ARX 参数；
- 已知时间滞后；
- 低噪声；
- 用于验证基本辨识正确性。

### 场景 S2：多输入不同滞后

要求：

- 2～4 个输入变量；
- 不同输入具有不同真实时滞；
- 用于验证多输入时滞估计。

### 场景 S3：长稳态、短动态

要求：

- 大量稳态区间；
- 少量阶跃响应；
- 少量斜坡响应；
- 少量脉冲响应；
- 用于验证动态区间筛选。

### 场景 S4：污染数据

注入：

- 缺失值；
- 尖峰异常；
- 冻结值；
- 漂移；
- 时间戳不规则；
- 重复时间戳；
- 强噪声。

### 场景 S5：高共线性

要求：

- 构造高度相关输入变量；
- 构造冗余输入变量；
- 保存真实变量依赖关系；
- 用于验证变量筛选。

### 场景 S6：异构激励（INNO-1.0 创新 1 增补）

要求：

- 大幅、重复的激励集中在少数输入（如 u1 动 6 次、幅度 ≈1.55）；
- 其余输入激励稀少且幅度小（u2/u3 各动 2 次、幅度 ≈0.6）；
- 各输入的激励时段**互不重叠**；
- 保存每个突发段的位置、形态与所涉变量。

理由：现有 S1–S5 全部是**同构激励**——每次扰动同时拉动所有输入，因此任何动态窗口携带的信息方向基本相同，无法暴露"按能量选段"的失效模式。S6 模拟真实工厂历史数据（操作员这段时间调这个阀、另一段时间调那个阀），是验证数据优选算法的关键场景。

实测结论：在 S6 上，按能量选段会把预算全花在重复的 u1 大动作上，u2/u3 对应列近似常数、设计矩阵实质秩亏，稳态增益被估成 0（误差 100%），条件数 3.97e+10。详见 `docs/experiments/identifiability-ablation.md`。

生成器参考实现见 `backend/scripts/benchmark_identifiability.py` 的 `_burst_plan()`，本阶段需将其并入 `app/services/simulation_service.py` 并输出标准真值文件。

每个仿真数据集生成真值文件：

```json
{
  "system_type": "MISO_ARX",
  "true_na": 2,
  "true_nb": [2, 2],
  "true_delays": [3, 8],
  "true_parameters": {},
  "dynamic_segments": [],
  "anomaly_indices": [],
  "missing_indices": [],
  "freeze_segments": [],
  "redundant_variables": [],
  "noise_level": 0.1,
  "seed": 42
}
```

### ARX 基线功能

实现：

- 滞后矩阵构造；
- 普通最小二乘；
- 岭回归；
- 一步预测；
- 按时间顺序划分数据；
- RMSE；
- MAE；
- `R²`；
- FIT；
- 参数估计误差；
- 结果持久化。

## 7.3 Gemini 任务

完成：

- 仿真数据生成页面；
- 仿真参数配置；
- 生成任务状态；
- 数据集列表；
- 数据集详情；
- 多变量时序图；
- 真值信息卡片；
- CSV 下载；
- 生成失败提示。

## 7.4 开发者验收

- [ ] 无噪声条件下参数接近真值。
- [ ] 增大噪声后误差合理增加。
- [ ] 固定随机种子后结果可复现。
- [ ] 数据划分没有随机打乱。
- [ ] 前端图表来自真实 API。
- [ ] 指标由后端实际计算。
- [ ] 真值文件与生成数据一致。

## 7.5 基线结果表

| 数据集 | 参数误差 | RMSE | MAE | R² | FIT | 是否通过 |
|---|---:|---:|---:|---:|---:|---|
| S1 |  |  |  |  |  |  |
| S2 |  |  |  |  |  |  |
| S3 |  |  |  |  |  |  |

## 7.6 Git 标签

```text
v0.3.0-simulation-baseline
```

---

# 8. 阶段 3：数据集管理与质量诊断

## 8.1 阶段目标

实现工业时序 CSV 的上传、解析、配置、诊断和版本管理。

## 8.2 GPT 任务

实现：

- CSV 上传；
- 扩展名校验；
- MIME 校验；
- 文件大小限制；
- SHA-256 去重；
- 编码识别；
- 分隔符识别；
- 时间戳解析；
- 字段类型推断；
- 输入变量配置；
- 输出变量配置；
- 数据预览；
- 数据删除；
- 原始文件保留；
- 数据版本管理。

质量诊断指标：

- 行数；
- 列数；
- 时间范围；
- 采样间隔；
- 不规则采样比例；
- 缺失率；
- 连续缺失长度；
- 重复时间戳；
- 常量列；
- 冻结段；
- 极值；
- 均值；
- 标准差；
- 分位数；
- 初步相关性；
- 数据完整性评分。

建议数据表：

```text
datasets
dataset_versions
dataset_columns
dataset_profiles
processing_runs
artifacts
operation_logs
```

## 8.3 Gemini 任务

完成：

- 数据集列表；
- 拖拽上传；
- 上传进度；
- 字段映射向导；
- 时间列选择；
- 输入变量选择；
- 输出变量选择；
- 数据质量概览；
- 缺失率柱状图；
- 采样间隔分布；
- 数据预览表格；
- 删除确认；
- 空状态；
- 失败状态。

## 8.4 异常文件测试

- [ ] 无时间列。
- [ ] 多个时间列。
- [ ] 非法时间格式。
- [ ] 重复列名。
- [ ] 全空列。
- [ ] 数据行不足。
- [ ] 重复上传。
- [ ] 超大文件。
- [ ] 中文列名。
- [ ] 非 UTF-8 编码。
- [ ] 不规则分隔符。
- [ ] 重复时间戳。

## 8.5 Git 标签

```text
v0.4.0-dataset-management
```

---

# 9. 阶段 4：数据清洗与时间规整

## 9.1 阶段目标

建立标准化的数据清洗工具库和不可覆盖原始数据的数据版本体系。

## 9.2 GPT 任务

建立统一算法工具接口：

```python
class AlgorithmTool:
    name: str
    input_schema: type
    output_schema: type

    def execute(self, context, parameters):
        ...
```

### 时间规整工具

- 时间排序；
- 重复时间戳处理；
- 固定周期重采样；
- 线性插值；
- 前向填充；
- 后向填充；
- 最大连续插值长度限制。

### 异常检测工具

- IQR；
- Hampel；
- Z-score；
- Isolation Forest；
- 冻结值检测；
- 简单漂移检测。

### 平滑工具

- 移动平均；
- Savitzky–Golay。

每次运行必须保存：

- 输入数据版本；
- 算法名称；
- 参数；
- 异常位置；
- 原始值；
- 处理后值；
- 处理原因；
- 输出数据版本；
- 执行时间；
- 失败原因。

禁止直接覆盖原始数据。

## 9.3 Gemini 任务

完成：

- 清洗策略配置；
- 算法选择；
- 参数表单；
- 算法说明；
- 清洗前后对比；
- 异常点标注；
- 缺失值热力图；
- 处理步骤时间线；
- 数据版本切换；
- 重新运行；
- 清洗后数据导出。

## 9.4 开发者验收

- [ ] 注入异常可以被识别。
- [ ] 正常动态响应不会被大量误删。
- [ ] 长缺失区间不会被无条件插值。
- [ ] 每个修改都有审计记录。
- [ ] 原始数据不可被覆盖。
- [ ] 处理前后行数合理。
- [ ] 处理前后时间范围合理。
- [ ] 清洗结果可复现。

## 9.5 Git 标签

```text
v0.5.0-cleaning
```

---

# 10. 阶段 5：高动态、高信噪比区间优选

## 10.1 阶段目标

在无标签条件下识别适合系统辨识的动态有效区间，并对候选区间进行质量排序。

## 10.2 GPT 任务

实现：

- 滑动窗口；
- 输入变化能量；
- 输出变化能量；
- 滚动方差；
- 局部斜率；
- 信噪比估计；
- 异常比例；
- 稳态惩罚；
- 区间长度约束；
- 相邻区间合并；
- 区间质量排序。

质量评分建议：

```text
Q =
输入变化能量得分
+ 输出变化能量得分
+ 信噪比得分
+ 输入输出响应关联得分
- 稳态惩罚
- 异常比例惩罚
- 区间过短惩罚
```

系统必须输出每个得分分量，不能只输出总分。

**口径变更（INNO-1.0 创新 1 / ALG-0.2 §2）**：上述加权质量分 `Q` **不再决定选择结果**，降级为报告中的可解释性对照列。主口径改为 D-最优子模优选：

```text
J(S) = log det( λI + Σ_{W∈S} Φ_Wᵀ Φ_W )
```

用 lazy greedy（CELF）求解，单调子模保证 `1-1/e` 近似比，边际增益用矩阵行列式引理做增量更新。每个被选区间输出以 bit 为单位的**信息增益**，取代不可解释的加权分数。

原因：加权权重无法辩护，且能量大 ≠ 信息新。实测在异构激励场景（S6）下，能量启发式在同等预算下自由仿真 FIT 下降 12.83 个百分点、稳态增益误差 100%，而 IDS 用 13.5% 的数据与全量数据持平。数据见 `docs/experiments/identifiability-ablation.md`。

**原型已完成**：`backend/algorithms/identifiability/selection.py`、`excitation.py`，本阶段工作为接入服务层与 API，不需从零实现。

**新增能力**：持续激励阶次前置判据（ALG-0.2 §3）。`PE_order(u_j) < na + nb_j` 时阻断建模并返回 `INSUFFICIENT_EXCITATION`，给出"该数据段仅满足 n 阶持续激励"的工程结论。

仿真评价指标：

- 区间 Precision；
- 区间 Recall；
- 区间 IoU；
- 被选数据比例；
- 动态信息保留率；
- 稳态数据剔除率；
- **信息保留率 `selected_log_det / baseline_log_det`**；
- **选择前后设计矩阵条件数对比**。

## 10.3 Gemini 任务

完成：

- 全局时序图；
- 动态区间高亮；
- 候选区间列表；
- 质量分排名；
- 得分构成；
- 阈值调整；
- 区间启用和禁用；
- 区间放大；
- 原始数据和优选数据切换。

## 10.4 开发者验收

在 S3 与 S6 仿真场景上检查：

- [ ] 能识别已知动态区间。
- [ ] 大部分稳态数据被剔除。
- [ ] 动态区间不是通过写死索引获得。
- [ ] 阈值变化后结果合理变化。
- [ ] 区间评分具有解释。
- [ ] 区间 Precision 和 Recall 有实际计算结果。
- [ ] 信息增益单调不增（子模性的可观测推论）。
- [ ] lazy greedy 与朴素贪心逐窗口结果一致。
- [ ] S6 异构激励下 IDS 不出现秩亏，能量对照组的失效可被复现。
- [x] 完整加权分（含 SNR 与响应关联分量）的对照实验已补齐（EXP-1.1 §3 结论 2b、§4；实测**证伪**了「加权分应优于纯能量法」的原判断，两者在 S6 上失效方式完全相同）。
- [x] `window_size` 与预算的敏感性扫描已完成（`EXP-3.0`，200 个工作点；并发现 D-最优自身的失效边界：预算/窗长 ≤ 2.3 时同样秩亏，已作为 `budget_advisory` 提示写入产品）。

## 10.5 Git 标签

```text
v0.6.0-segment-selection
```

---

# 11. 阶段 6：时滞分析与共线性处理

## 11.1 阶段目标

自动估计输入输出时滞，完成时滞补偿，并识别强共线性和冗余变量。

## 11.2 GPT 任务

### 时滞分析

实现：

- 数据标准化；
- 去趋势；
- 互相关函数；
- 最大滞后范围；
- 候选峰值提取；
- 多输入分别估计；
- ARX 验证集指标复核；
- 最终时滞选择；
- 时滞补偿；
- 无效边界数据裁剪。

注意：

> 互相关最大值只能作为候选时滞，不能直接作为最终答案。

最终时滞应结合验证集辨识效果确定。

### 共线性分析

实现：

- Pearson 相关系数；
- Spearman 相关系数；
- VIF；
- 条件数；
- 高相关变量分组；
- 冗余变量识别；
- 变量删除建议；
- 变量保留理由。

进阶功能：

- PCA；
- PLS；
- Lasso。

第一版可暂不实现进阶功能。

### 输出内容

```text
变量名称
变量对
Pearson 相关系数
Spearman 相关系数
VIF
候选时滞
最终时滞
建议保留或删除
判断依据
处理后的变量集合
```

## 11.3 Gemini 任务

完成：

- 相关性热力图；
- 滞后相关曲线；
- 时滞关系表；
- VIF 排名；
- 变量关系网络；
- 变量保留和删除建议；
- 用户确认面板；
- 处理前后对比；
- 解释详情抽屉。

## 11.4 开发者验收

使用 S2 和 S5 检查：

- [ ] 时滞估计误差。
- [ ] 冗余变量识别率。
- [ ] 共线变量识别结果。
- [ ] 处理前后参数稳定性。
- [ ] 处理前后验证集指标。
- [ ] 删除变量后没有明显信息损失。
- [ ] 时滞补偿过程可以追溯。

## 11.5 Git 标签

```text
v0.7.0-delay-collinearity
```

---

# 12. 阶段 7：完整系统辨识与模型评价

## 12.1 阶段目标

完善 ARX 模型训练、预测、仿真、指标计算和残差检验。

## 12.2 GPT 任务

ARX 参数配置：

- `na`；
- 每个输入变量的 `nb`；
- 每个输入变量的 `nk`；
- 最小二乘；
- 岭回归；
- 正则化参数；
- 模型阶次搜索。

实现：

- 一步预测；
- 自由仿真；
- 模型持久化；
- 模型版本；
- 参数输出；
- 可复现随机种子。

评价指标：

- RMSE；
- MAE；
- `R²`；
- NRMSE；
- FIT；
- 参数数量；
- AIC；
- BIC；
- 残差均值；
- 残差自相关；
- 残差与输入互相关；
- 训练时间；
- 验证时间。

**主指标变更（INNO-1.0 创新 2 / ALG-0.2 §5）**：**自由仿真 FIT 取代一步预测 FIT 成为主指标**。一步预测 FIT 保留为诊断量，报告必须同时展示两者及差值 `fit_gap`——`fit_gap` 大意味着模型靠输出自相关"抄答案"。

理由（实测，S6 场景 10 组种子）：策略间一步预测 FIT 跨度仅 0.46 个百分点，而自由仿真 FIT 跨度 12.83 个百分点，**区分度相差 27.7 倍**。一个参数误差 14.82%、稳态增益完全估错的废模型，一步预测 FIT 仍有 96.63。以一步预测 FIT 作为阶段 8 目标函数，闭环寻优本质上是在噪声里随机游走。

新增入库前检查（不通过则模型不进入正式版本）：

- **稳定性**：`A(q)` 全部极点模 < 1，否则 `MODEL_UNSTABLE`；
- **稳态增益** `K_j = Σb_j / (1+Σa)`：与工程师先验冲突时 `PRIOR_CONSTRAINT_VIOLATED`；
- **残差白度**：自相关落在 `±1.96/√N` 内的滞后比例，< 0.6 时告警。

开环不稳定或积分型过程自由仿真会发散，改用 `N` 步预测 FIT（默认 20 步）并在报告中标注口径。

**原型已完成**：`backend/algorithms/identifiability/validation.py`。

**新增能力（INNO-1.0 创新 6 / ALG-0.2 §6）**：先验约束辨识。工程师自然语言先验（增益符号、增益区间、时滞下界、自衡性）经 Agent 解析为结构化约束，进入带约束最小二乘与搜索空间边界。优先用投影法/等式消元实现，SciPy QP 为可选路径。

必须严格区分：

- 训练集指标；
- 验证集指标；
- 测试集指标。

## 12.3 Gemini 任务

完成：

- 模型配置页；
- 模型运行页；
- 真实值与预测值图；
- 残差曲线；
- 残差自相关图；
- 指标卡片；
- 参数表；
- 模型版本对比；
- 训练、验证、测试切换；
- 模型参数下载。

## 12.4 对比实验

必须比较：

```text
原始全部数据
清洗后数据
动态优选数据
时滞补偿数据
共线性处理数据
完整处理数据
```

## 12.5 开发者验收

- [ ] 完整流程优于原始基线。
- [ ] 验证指标没有使用训练指标代替。
- [ ] 测试集在模型选择过程中未被使用。
- [ ] 残差分析真实计算。
- [ ] 模型参数可导出。
- [ ] 模型结果可复现。
- [ ] 失败模型不会写入正式版本。

## 12.6 Git 标签

```text
v0.8.0-identification
```

---

# 13. 阶段 8：闭环参数自动寻优

## 13.1 阶段目标

使用系统辨识效果作为反馈，自动搜索数据预处理和模型配置的最佳组合。

## 13.2 GPT 任务

使用 Optuna TPE 建立寻优系统。

### 搜索参数

- 重采样周期；
- 插值方式；
- 异常检测算法；
- 异常阈值；
- 平滑窗口；
- 动态窗口长度；
- 动态质量阈值；
- 最短区间长度；
- 候选时滞；
- 变量保留策略；
- `na`；
- `nb`；
- `nk`；
- 岭回归正则化系数。

### 目标函数

不能只优化训练拟合度。

建议综合：

```text
目标函数 =
验证集 FIT 奖励
- 验证集 RMSE 惩罚
- 残差相关性惩罚
- 模型复杂度惩罚
- 有效数据不足惩罚
```

**口径变更（INNO-1.0 创新 2 / ALG-0.2 §7.1）**：上式中的「验证集 FIT」必须是**验证集自由仿真 FIT**，不是一步预测 FIT（理由见阶段 7）。不稳定模型直接判为失败 Trial。目标函数改为：

```text
objective = 0.45 · clip(FIT_sim_val / 100, -1, 1)
          - 0.20 · clip(NRMSE_sim_val, 0, 3)
          - 0.15 · gain_prior_penalty
          - 0.10 · (1 - residual_whiteness_ratio)
          - 0.10 · complexity_penalty
```

可选支持 NSGA-II 三目标（拟合精度 / 增益可信度 / 复杂度），输出 Pareto 前沿供工程师选点。

### 寻优加速（INNO-1.0 创新 3 / ALG-0.2 §7.3-7.4）

- **血缘即缓存**：`version_key = SHA256(parent_key ‖ tool_name ‖ tool_version ‖ canonical_json(params) ‖ code_commit)`。相邻 Trial 通常只改流水线末端参数，前置中间版本可直接复用。这不是新增缓存系统，而是把 `scope-freeze.md` §2.3 已要求的血缘元数据直接当内容寻址键用。
- **多保真剪枝**：Optuna `HyperbandPruner`，Trial 先在 25% 窗口预算上评估。
- **搜索空间分层**：按"是否影响前置产物"分层，最大化缓存命中率。

必须上报：`cache_hit_rate`、`speedup_ratio`、`mean_trial_seconds`、`pruned_trial_ratio`。

### 自演进策略记忆库（INNO-1.0 创新 4 / ALG-0.2 §7.5）

赛题原文要求「具备**自演进**能力的智能预处理引擎」。单次会话内的迭代不构成自演进，必须跨数据集积累：

- **数据画像指纹**：采样周期、缺失率、不规则率、信噪比、动态段占比、输入变量数、共线性条件数、时滞跨度、PE 阶次分布，按训练集稳健分位数归一化；
- **记忆库**：每次寻优结束落库 `(fingerprint, best_params, achieved_metrics, dataset_id, created_at)`；
- **热启动**：新数据集经 k-NN 检索，用 `study.enqueue_trial()` 注入历史最优参数；
- **回退**：热启动 10 轮内未超越基线则回退默认搜索空间并记录；
- **防泄漏**：记忆库不保存任何测试集信息；同一数据集的历史记录不得用于其自身热启动。

出厂预置 S1–S6 的寻优结果，避免冷启动时功能不可见。

阶段 11 必须补消融实验：热启动 vs 冷启动的收敛 Trial 数对比（≥5 组数据集）。

### 任务控制

实现：

- 创建任务；
- 暂停；
- 取消；
- 失败重试；
- 断点恢复；
- Trial 持久化；
- 最佳参数保存；
- 最佳数据版本保存；
- 收敛判断；
- 最大试验次数；
- 超时限制；
- 基线自动比较。

每个 Trial 保存：

```text
输入数据版本
参数组合
中间数据版本
训练指标
验证指标
测试指标
目标函数值
运行时间
随机种子
执行状态
失败原因
```

## 13.3 Gemini 任务

完成：

- 寻优任务创建；
- 搜索空间配置；
- 实时运行状态；
- 收敛曲线；
- Trial 表格；
- 最优值变化；
- 参数重要性；
- 最佳方案详情；
- 基线与最优方案对比；
- 暂停；
- 取消；
- 最佳数据集导出。

## 13.4 开发者验收

至少使用三个仿真场景：

- [ ] 最佳验证指标优于基线。
- [ ] 测试集指标没有明显下降。
- [ ] 重复运行结果处于合理波动范围。
- [ ] 失败 Trial 不会导致任务崩溃。
- [ ] 每个 Trial 可以追溯。
- [ ] 最佳方案可以重新执行。
- [ ] 搜索过程没有使用测试集调参。

## 13.5 Git 标签

```text
v0.9.0-closed-loop-optimization
```

---

# 14. 阶段 9：Agent 智能编排

## 14.1 阶段目标

让用户通过自然语言提出建模数据处理需求，由 Agent 生成受约束的工作流并调用真实算法。

## 14.2 Agent 工具白名单

```text
profile_dataset
align_and_resample
detect_anomalies
clean_data
detect_dynamic_segments
estimate_delays
analyze_collinearity
select_variables
fit_arx
evaluate_model
optimize_pipeline
generate_report
export_dataset
```

## 14.3 GPT 任务

Agent 分为四层。

### Planner

负责：

- 识别用户目标；
- 识别数据集；
- 生成结构化计划；
- 填写必要参数。

### Validator

校验：

- 工具是否合法；
- 参数是否合法；
- 数据集是否存在；
- 前置步骤是否完成；
- 工具顺序是否正确；
- 参数是否越界；
- 是否需要人工确认。

**能力升级（INNO-1.0 创新 5 / ALG-0.2 §9）**：Validator 从"逐项检查"升级为**执行前的静态数据流分析**，产出机器可检验的合规证明并附在报告首页：

```text
合规证明 PROOF-2026-08-07-a91f
[✓] 全部工具调用均在白名单内
[✓] 测试集在方案冻结前零触及（数据流分析覆盖 7 个中间数据版本）
[✓] 全部拟合型变换（标准化/异常阈值/插值参数）仅由训练集估计
[✓] 无跨分区边界插值
[✓] 高影响操作（删除 u3）已获人工确认，确认记录 #c-0042
[✓] 计划 DAG 无环，依赖完备
签名：sha256:91f3...  代码版本：v1.1.0-innovation
```

任一项失败则计划不得执行，返回 `COMPLIANCE_PROOF_FAILED`。`scope-freeze.md` §5.3 已定义全部约束，本项是把**文档里的规定升级为每次运行自动出具的证明**。对工业控制企业而言，"可审计"的分量不亚于"精度高"。

### Executor

负责：

- 调用真实 Python 算法；
- 保存工具输入；
- 保存工具输出；
- 保存执行时间；
- 保存异常。

禁止：

- 大模型自行伪造计算结果；
- 大模型任意执行 Python；
- 大模型修改底层指标；
- 大模型绕过工具白名单。

### Controller

读取运行结果后决定：

- 接受结果；
- 调整参数；
- 继续寻优；
- 停止；
- 请求用户确认。

### 结构化计划示例

```json
{
  "goal": "生成适用于 ARX 建模的高质量数据",
  "dataset_id": "ds_001",
  "steps": [
    {
      "tool": "profile_dataset",
      "parameters": {}
    },
    {
      "tool": "detect_dynamic_segments",
      "parameters": {
        "window_size": 60,
        "min_segment_length": 120
      }
    },
    {
      "tool": "estimate_delays",
      "parameters": {
        "max_lag": 60
      }
    },
    {
      "tool": "optimize_pipeline",
      "parameters": {
        "max_trials": 50,
        "model_type": "ARX"
      }
    }
  ]
}
```

必须通过 Pydantic 校验。

### Agent 审计内容

保存：

- 原始用户指令；
- 原始大模型输出；
- JSON 修复记录；
- 最终结构化计划；
- 每个工具的输入；
- 每个工具的输出；
- 执行耗时；
- 异常；
- 重试记录；
- 最终结论；
- 停止原因。

## 14.4 Gemini 任务

完成 Agent 工作台：

- 对话输入；
- 示例指令；
- Agent 计划卡片；
- DAG；
- 执行步骤时间线；
- 当前步骤；
- 工具参数；
- 工具输出；
- 失败状态；
- 重试状态；
- 人工确认；
- 最终结论；
- 跳转到算法详情；
- 运行历史。

不能只显示聊天气泡。

评委必须能够看到：

```text
Agent 理解了什么
→ 调用了什么工具
→ 使用了什么参数
→ 得到了什么结果
→ 为什么继续寻优
→ 为什么停止
```

## 14.5 Agent 测试集

至少准备 30 条指令，覆盖：

- 正常指令；
- 参数明确指令；
- 模糊指令；
- 缺少数据集；
- 非法参数；
- 不支持的算法；
- 越界参数；
- 恶意指令；
- 需要人工确认；
- 工具执行失败；
- 任务取消；
- 中途恢复。

## 14.6 Git 标签

```text
v1.0.0-agent
```

---

# 15. 阶段 10：报告、数据交付和演示闭环

## 15.1 阶段目标

输出完整的建模数据优选报告、优选数据集、模型和可复现信息。

## 15.2 GPT 任务

报告内容：

1. 数据集基本信息；
2. 字段和采样信息；
3. 数据质量问题；
4. 清洗策略；
5. 异常处理统计；
6. 清洗前后对比；
7. 动态区间选择；
8. 动态区间质量评分；
9. 时滞分析；
10. 共线性分析；
11. 变量保留和删除结果；
12. ARX 模型配置；
13. 基线模型结果；
14. 闭环寻优过程；
15. 最佳参数；
16. 训练集指标；
17. 验证集指标；
18. 测试集指标；
19. 残差检验；
20. 系统局限；
21. 风险提示；
22. 可复现信息；
23. **合规证明**（阶段 9 生成，见 ALG-0.2 §9）。

**生成机制变更（INNO-1.0 创新 5 / ALG-0.2 §8）**：**LLM 禁止输出数字字面量**，只能输出占位符，由渲染器从运行数据库解析替换：

```text
最优方案在验证集上的自由仿真 FIT 达到 {{run:r_8f2a.metrics.free_run_fit}}，
相比基线的 {{run:r_1c04.metrics.free_run_fit}} 提升明显。
```

校验流程：LLM 产出草稿 → 校验器扫描未被占位符包裹的数字字面量（白名单：条目编号、章节号）→ 命中即判违规，记审计日志并重新生成（最多 3 次，仍失败回退模板化报告）→ 渲染器替换为真实值，附 `run_id` 与输入校验和的可点击溯源。

错误码 `REPORT_CONTAINS_UNBOUND_NUMERAL`。这把"相信大模型不撒谎"变成了"大模型在结构上不可能撒谎"。渲染在后端同步完成，不经过第二次 LLM 调用，不影响演示流畅度。

导出文件：

```text
优选后的 CSV
处理流水线 JSON
模型参数 JSON
分析报告 HTML
分析报告 PDF
运行日志
图表 PNG
图表 SVG
测试摘要
```

## 15.3 Gemini 任务

完成：

- 报告中心；
- 报告列表；
- 报告预览；
- 图表目录；
- 章节导航；
- 导出格式选择；
- 数据包下载；
- 结果分享页面；
- 演示模式；
- 录屏友好布局。

## 15.4 完整演示流程

```text
上传或生成工业数据
→ 查看数据质量
→ 输入自然语言任务
→ Agent 生成工作流
→ 数据清洗
→ 动态区间优选
→ 时滞分析
→ 共线性处理
→ ARX 系统辨识
→ 闭环参数寻优
→ 查看最优结果
→ 查看模型残差
→ 导出报告
→ 导出优选数据集
```

## 15.5 Git 标签

```text
v1.1.0-report-delivery
```

---

# 16. 阶段 11：测试、实验与竞赛交付

## 16.1 阶段目标

完成正式实验、系统测试、演示材料和比赛发布包。

## 16.2 GPT 任务

### 后端测试

- 算法单元测试；
- API 集成测试；
- 数据库事务测试；
- 文件上传安全测试；
- 任务取消测试；
- 任务恢复测试；
- Agent 参数校验测试；
- 大模型失败降级；
- 超时处理；
- 日志脱敏；
- Docker 部署测试。

### 基线对比实验

比较：

1. 原始数据直接 ARX；
2. 人工固定预处理；
3. 自动闭环寻优。

### 消融实验

分别去掉：

- 异常处理；
- 动态区间筛选；
- 时滞补偿；
- 共线性处理；
- 闭环寻优。

**创新点消融（INNO-1.0 创新 7）**，每项都必须给出可复现的量化对照：

| 实验 | 对照组 | 指标 | 状态 |
|---|---|---|---|
| 数据优选 | full / energy / weighted / **ids** | 参数误差、自由仿真 FIT、稳态增益误差、条件数 | **已完成**，见 `EXP-1.1` / `EXP-2.1` |
| 指标区分度 | 一步预测 FIT vs 自由仿真 FIT | 策略间跨度之比 | **已完成**（27.7×） |
| 完整加权分对照 | 加权分（含 SNR/响应关联） vs ids | 同上 | **已完成**：S6 上加权分与能量法同样秩亏（14.81% vs 14.82%）；S3 上加权分反而最优（3.75% vs ids 4.80%），已据实写入 `EXP-2.1` §2 结论 3 |
| 时滞估计 | 朴素互相关 vs 预白化互相关 | 各输入 `nk` 命中率 | 待做 |
| 闭环加速 | 无缓存 vs 血缘缓存 + Hyperband | 缓存命中率、端到端加速比、单 Trial 耗时 | 待做 |
| 自演进 | 冷启动 vs 记忆库热启动 | 达到同等目标值的 Trial 数（≥5 组数据集） | 待做 |
| 先验约束 | 无约束 LS vs 约束 LS | 增益符号正确率、不稳定模型比例 | 待做 |

### 一键自评测基准（INNO-1.0 创新 7）

把上述实验协议做成**产品内的一个按钮**：评委点击"运行自评测基准"，系统当场执行并生成对照表。

理由：绝大多数参赛作品无法证明自己有效——没有真值，只能展示"曲线拟合上了"。本项目有 S1–S6 仿真集和完整真值 JSON，**可以量化地自证有效**。开发成本几乎为零，因为这些实验本来就要做，只是把执行器接到一个按钮上。

执行器已存在：`backend/scripts/benchmark_identifiability.py`（支持 `--repeats` 多种子聚合与 `--json` 结构化输出），阶段 11 需将其包装为 API + 前端页面。

### 鲁棒性实验

改变：

- 噪声强度；
- 缺失率；
- 异常比例；
- 稳态比例；
- 共线性强度；
- 真实时滞长度；
- 数据长度；
- 输入变量数量。

### 性能测试

记录：

- 电脑配置；
- CPU；
- 内存；
- 操作系统；
- 数据行数；
- 变量数量；
- 数据文件大小；
- 处理时间；
- 内存占用；
- API 延迟；
- Trial 数量；
- 大模型响应时间；
- 平均值；
- 中位数；
- 最大值。

## 16.3 Gemini 任务

完成：

- 前端组件测试；
- API 异常状态；
- 空数据状态；
- 超长任务状态；
- 页面刷新恢复；
- 多分辨率适配；
- 图表性能测试；
- E2E 主流程测试；**已完成**：`frontend/e2e/closed-loop.spec.ts`，Playwright + 预装 Chromium 9 项，真实浏览器驱动真实后端（`backend/scripts/serve_e2e.py` 提供用完即弃的 SQLite 实例，不依赖 Docker）；
- 演示模式优化。

## 16.4 开发者竞赛交付物

准备：

- [ ] 项目概要介绍；
- [ ] 项目简介 PPT；
- [ ] 项目详细方案；
- [ ] 演示视频；
- [ ] 系统使用说明；
- [ ] 技术架构说明；
- [ ] 算法说明；
- [ ] 本地部署说明；
- [ ] 测试报告；
- [ ] 实验报告；
- [ ] 开发过程记录；
- [ ] AI 使用说明；
- [ ] 开源依赖清单；
- [ ] 项目源代码；
- [ ] 演示数据；
- [ ] 仿真真值文件；
- [ ] 答辩问题库；
- [ ] 一键启动脚本。

## 16.5 最终 Git 标签

```text
v1.2.0-competition-release
```

---

# 17. 推荐开发排期

## 17.1 9～10 周版本

| 周次 | 阶段 | 核心结果 |
|---|---|---|
| 第 1 周 | 阶段 0～1 | 需求、架构、骨架、API 规范 |
| 第 2 周 | 阶段 2 | 仿真数据和 ARX 基线 |
| 第 3 周 | 阶段 3～4 | 数据管理、诊断、清洗 |
| 第 4 周 | 阶段 5 | 动态区间优选 |
| 第 5 周 | 阶段 6～7 | 时滞、共线性、系统辨识 |
| 第 6 周 | 阶段 8 | 闭环寻优 |
| 第 7 周 | 阶段 9 | Agent 编排 |
| 第 8 周 | 阶段 10 | 报告和完整演示 |
| 第 9 周 | 阶段 11 | 测试、实验和优化 |
| 第 10 周 | 交付 | 视频、PPT、文档和发布 |

## 17.2 6～7 周压缩版本

需要主动删除：

- PCA；
- PLS；
- Lasso；
- 深度学习辨识；
- 复杂登录权限；
- 多租户；
- 移动端；
- 在线实时数据；
- 任意代码执行；
- 多模型辨识；
- 复杂报告编辑器。

保留：

- 仿真数据；
- CSV 上传；
- 数据诊断；
- 清洗；
- 动态区间优选；
- 时滞分析；
- VIF 共线性分析；
- ARX；
- Optuna；
- Agent 白名单工具；
- 报告导出；
- 完整实验。

---

# 18. GPT 与 Gemini 标准交接流程

每个阶段严格执行：

```text
1. 将当前阶段任务交给 GPT。
2. GPT 输出设计、数据库、API、算法、代码和测试。
3. 本地运行 GPT 后端代码。
4. 修复后端错误。
5. 运行全部后端测试。
6. 导出最新 openapi.json。
7. 将 openapi.json 和示例响应交给 Gemini。
8. Gemini 开发对应前端。
9. 本地运行前端测试和构建。
10. 完成前后端联调。
11. 检查页面是否使用真实数据。
12. 更新 PHASE_STATUS.md。
13. 更新 CHANGELOG.md。
14. Git 提交。
15. 打阶段标签。
```

---

# 19. 每阶段交给 GPT 的统一提示词

```text
你现在负责 2026“数字马力杯”A14 项目第 X 阶段的后端、算法和测试开发。

项目名称：
IndusOpt 工模智优——面向流程工业系统辨识的自主建模数据工程师 Agent。

你的工作范围仅限 backend、algorithms、tests、alembic、docs 中与后端有关的内容，不得修改 frontend 目录。

在编码前，必须先输出：

1. 本阶段目标；
2. 本阶段对应的官方要求；
3. 本阶段开发范围；
4. 本阶段明确不做的内容；
5. 输入与输出；
6. 数据库变化；
7. API 清单；
8. 算法定义；
9. 文件变更清单；
10. 测试方案；
11. 验收标准；
12. 风险；
13. 回滚方式。

设计确认自洽后，再开始编码。

通用要求：

- 不使用伪代码；
- 不省略关键文件；
- 不修改 frontend；
- 使用 Python 3.11+；
- 使用 FastAPI、Pydantic 2、SQLAlchemy 2；
- 数据库变化必须提供 Alembic migration；
- API 必须提供明确请求和响应模型；
- 所有核心算法必须有单元测试；
- 所有接口必须有异常处理；
- 所有运行必须记录 request_id；
- 所有结果必须由代码真实计算；
- 禁止由大模型伪造算法指标；
- 禁止使用训练集指标代替验证集指标；
- 测试集不得参与参数选择；
- 原始数据不得被直接覆盖；
- 所有处理必须形成数据版本；
- 大模型不得任意执行 Python；
- Agent 只能调用白名单工具；
- 固定随机种子后结果必须可复现；
- 完成后运行 pytest、Ruff 和 Mypy；
- 更新 OpenAPI；
- 更新阶段交付文档；
- 给出完整运行命令；
- 给出本阶段验收结果。

在未完成当前阶段全部验收前，不进入下一阶段。
```

---

# 20. 每阶段交给 Gemini 的统一提示词

```text
你现在负责 2026“数字马力杯”A14 项目第 X 阶段的前端开发。

项目名称：
IndusOpt 工模智优——面向流程工业系统辨识的自主建模数据工程师 Agent。

后端 OpenAPI、接口字段和示例响应已经冻结。你不得自行修改接口，不得虚构后端字段，不得修改 backend 目录。

在编码前，必须先输出：

1. 页面目标；
2. 用户操作流程；
3. 页面结构；
4. 组件结构；
5. API 对应关系；
6. 数据类型；
7. 加载状态；
8. 空状态；
9. 错误状态；
10. 图表类型；
11. 图表数据映射；
12. 文件变更清单；
13. 前端测试方案；
14. 验收标准。

通用要求：

- 不修改 backend；
- 使用 Vue 3；
- 使用 Vite；
- 使用 TypeScript；
- 使用 Pinia；
- 使用 Vue Router；
- 使用 Axios；
- 使用 Element Plus；
- 使用 ECharts；
- 所有业务数据必须来自真实 API；
- 禁止固定假数据冒充算法结果；
- 所有异步操作必须有加载状态；
- 所有异步操作必须有失败状态；
- 所有图表必须支持空数据；
- 页面刷新后任务状态应能够恢复；
- 页面风格专业、克制，接近工业软件；
- 不使用大量发光效果；
- 不使用大面积渐变；
- 不使用过度玻璃拟态；
- 不使用无意义装饰动画；
- 算法结论必须展示依据；
- 重要指标必须注明所属训练集、验证集或测试集；
- 图表坐标轴、单位、图例必须完整；
- 完成后运行前端测试；
- 完成后运行 pnpm build；
- 输出本阶段验收结果。

在后端接口未冻结前，不得开发对应正式业务页面。
```

---

# 21. 每阶段状态记录模板

在根目录维护 `PHASE_STATUS.md`：

```markdown
# 当前阶段状态

## 当前阶段

阶段 X：阶段名称

## 开始时间

YYYY-MM-DD

## 当前状态

- [ ] 未开始
- [ ] 设计中
- [ ] 后端开发中
- [ ] 后端测试中
- [ ] 前端开发中
- [ ] 联调中
- [ ] 验收中
- [ ] 已完成

## 已完成

- 

## 未完成

- 

## 当前问题

- 

## 关键决策

- 

## 数据库版本

- Alembic Revision：

## API 版本

- OpenAPI Version：

## 测试结果

- pytest：
- Ruff：
- Mypy：
- 前端测试：
- 前端构建：

## 验收结论

- [ ] 通过
- [ ] 不通过

## 下一阶段进入条件

- 
```

---

# 22. 开发红线

整个项目禁止出现以下情况：

1. 大模型直接编造算法结果。
2. 前端用假数据冒充正式结果。
3. 使用训练集成绩作为最终效果。
4. 在测试集上反复调参。
5. 直接覆盖原始数据。
6. 没有记录数据处理版本。
7. 没有保存算法参数。
8. 没有固定随机种子。
9. Agent 任意执行 Python。
10. Agent 可以执行任意 SQL。
11. 互相关最大值直接作为最终时滞，不做验证。
12. 动态区间通过写死索引获得。
13. 闭环寻优只播放预设动画。
14. 只展示最好的一次结果，不保存失败 Trial。
15. 算法指标无法通过代码重新生成。
16. GPT 和 Gemini 同时修改同一个目录。
17. 后端接口未冻结就开发正式前端。
18. 未完成阶段验收就进入下一阶段。
19. 为了页面数量扩展无关业务模块。
20. 最后阶段才补测试和开发记录。

---

# 23. 第一阶段立即执行清单

现在从阶段 0 开始。

## 交给 GPT

- [ ] 官方需求拆解；
- [ ] 项目范围冻结；
- [ ] 用例设计；
- [ ] 系统架构；
- [ ] 数据流；
- [ ] 算法规范；
- [ ] 评测协议；
- [ ] OpenAPI 规划；
- [ ] 阶段验收条件。

## 交给 Gemini

阶段 0 暂时只做：

- [ ] 用户角色；
- [ ] 页面信息架构；
- [ ] 页面流程；
- [ ] 低保真原型；
- [ ] 工业软件设计规范；
- [ ] 图表规划。

## 阶段 0 完成前禁止

- [ ] 禁止正式开发 Agent；
- [ ] 禁止正式开发闭环寻优；
- [ ] 禁止开发完整业务页面；
- [ ] 禁止一次性生成整个系统；
- [ ] 禁止临时增加无关功能。
